{
  "name": "ens-compute",
  "version": "0.1.0",
  "description": "ENS Compute: Resolution as Computation - Verifiable computation endpoints for ENS names",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "setup": "hardhat run scripts/setup.js",
    "gateway": "node gateway/server.js",
    "client": "node examples/client.js",
    "cli": "node cli/index.js",
    "ui:dev": "cd ui && npm run dev",
    "ui:build": "cd ui && npm run build",
    "dev": "concurrently \"hardhat node\" \"npm run gateway\""
  },
  "keywords": [
    "ens",
    "ethereum",
    "ccip-read",
    "verifiable-computation",
    "oracle",
    "faas"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "ethers": "^6.9.0",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "axios": "^1.6.2",
    "concurrently": "^8.2.2",
    "commander": "^11.1.0"
  }
}

